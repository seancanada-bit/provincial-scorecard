# Copy to .env.local for local development.
# In production this is set as a GitHub Actions secret (VITE_API_URL).

# Your Render backend URL — no trailing slash
VITE_API_URL=https://provincial-scorecard-api.onrender.com
